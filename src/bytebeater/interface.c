#include "interface.h"

int interface_setup(beat_context* ctx){
	//set data pins modes
	//start and empty screen
	//start UART keyboard listener
	//start MIDI listener
	return 0;
}

int interface(beat_context* ctx){
	//read all sensors
	//modify ctx
	return 0;
}
